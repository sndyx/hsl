fn all_builtins_test() {
    action_bar("Hello, world!")
    balance_team()
    cancel_event()
    clear_effects()
    close_menu()
    effect("Regeneration", 60, 1, true)
    enchant_held_item("Sharpness", 5)
    exit()
    fail_parkour()
    give_exp_levels(10)
    give_item(item {}, true)
    heal()
    kill()
    message("Hello, world!")
    open_menu("my_menu")
    parkour_checkpoint()
    pause(10)
    remove_held_item()
    remove_item(item {})
    reset_inventory()
    send_to_lobby("Main Lobby")
    set_compass_target(<1, 2, 3>)
    set_gamemode("Survival")
    set_group("my_group")
    set_layout("my_layout")
    set_team("my_team")
    sound("Note Pluck")
    spawn()
    title("Hello, ", "world", 1, 1, 1)
    tp(<1, 2, 3>)
}